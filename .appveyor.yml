version: 0.0.{build}-{branch}

branches:
  only:
    - main
    - AppVeyor-Test 
    
init:
  - git config --global core.autocrlf input
  
image:
  - Visual Studio 2019   
  - Ubuntu         

platform:
  - x86
  - x64

configuration:
  - Debug
  - Release

install:
  - mkdir build  

#build_script:
